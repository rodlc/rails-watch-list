<!DOCTYPE html>
<html>
  <head>
    <title>MyMDB</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-sm navbar-lewagon">
      <div class="container">
        <%= link_to root_path, class: "navbar-brand d-flex align-items-center gap-2 text-decoration-none" do %>
          <span style="font-size: 28px; line-height: 1;">🎬</span>
          <span class="fw-bold" style="color: #1D1D1F; font-size: 20px;">MyMDB</span>
        <% end %>

        <div class="d-flex align-items-center">
          <%= link_to new_list_path, class: "btn-apple-black" do %>
            <span style="font-size: 18px; font-weight: 400; line-height: 0;">+</span> 
            <span>New list</span>
        <% end %>
        </div>
      </div>
    </nav>

    <!-- PLUS DE PADDING ICI. La bannière va toucher le haut de l'écran. -->
    <%= yield %>
  </body>
</html>
